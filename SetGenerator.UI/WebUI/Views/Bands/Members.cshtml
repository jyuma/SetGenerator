
@model SetGenerator.WebUI.ViewModels.MemberViewModel

@if (!Request.IsAuthenticated)
{
    Url.Action("Index", "Home");
}

@{
    ViewBag.Title = "Manage Members";
}

<script type="text/javascript">
    $(document).ready(function() {
        bands.members.init(
        {
            bandId: '@Model.BandId'
        });
    });
</script>

<div class="panel panel-default">
    <div class="panel-heading">
        <div class="row">
            <div class="col-sm-10">
                <h3><i>@Model.BandName</i> Members</h3>
                <a style="padding: 5px 0 0 0" href="#" onclick="location.href = '@Url.Action("Index", "Bands", new {@id = @Model.BandId})'">Back To List</a>
            </div>
            <div class="pull-right record-total">
                <label>Total:</label> <span data-bind="text: totalMembers()"></span>
            </div>
        </div>
        <ul class="nav nav-pills">
            <li>
                <input type="button" class="btn btn-success" value="Add Member" data-bind="click: showEditDialog" />
            </li>
            <li class="navbar-right">
                <div id="ddlColumns" class="dropdown">
                    <button class="btn btn-default dropdown-toggle" type="button" id="btnShowHideColumns" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                        Show Columns
                        <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="btnShowHideColumns" data-bind="foreach: columns">
                        <li data-bind="if: !alwaysVisible">
                            <div>
                                <input type="checkbox" data-bind="checked: isVisible, attr: { id: 'chk_' + dataMember }, css: 'checkbox-inline'" /> <label for="" data-bind="text: title"></label>
                            </div>
                        </li>
                    </ul>
                </div>
            </li>
        </ul>
    </div>
    <div class="panel-body">
        <table class="table table-striped table-bordered table-hover" id="tblMember">
            <thead>
                <tr>
                    <th class="col-glyphicon"></th>
                    <th class="col-glyphicon"></th>
                    <th class="col-link-setlist"></th>
                    <!-- ko foreach: columns -->
                    <th data-bind="visible: isVisible, click: $parent.sort, attr: { id: 'member-col-' + dataMember } "><a href="#" data-bind="text: title"></a></th>
                    <!-- /ko -->
                </tr>
            </thead>

            <tbody data-bind="foreach: membersTable">
                <tr data-bind="click: $parent.highlightRow, attr: {id: 'row_' +  id}">
                    <td><a class="glyphicon glyphicon-remove" href="#" data-bind="click: $parent.showDeleteDialog"></a></td>
                    <td><a class="glyphicon glyphicon-pencil" href="#" data-bind="click: $parent.showEditDialog"></a></td>
                    <td><a data-bind="attr: { href: '#' }, click: $parent.showInstruments">Instruments</a></td>
                    <!-- ko foreach: $parent.columns -->
                    <td data-bind="text: (dataMember.indexOf('updatedate') >= 0) ? moment($parent[dataMember]).format('ll') : $parent[dataMember], visible: isVisible, css: { 'col-audit': dataMember.indexOf('updateuser') >= 0 || dataMember.indexOf('updatedate') >= 0 }"></td>
                    <!-- /ko -->
                </tr>
            </tbody>
        </table>
    </div>
</div>